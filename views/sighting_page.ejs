<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/login.css'>
    <link rel='stylesheet' href='/stylesheets/sightings.css'>
</head>

    <header>
    <h1>BIRD</h1>
    </header>
        <div class="user">Welcome, <%= nickname %></div>
<body>
<div class="container">
    <div class="search-container">
        <form action="/sighting/fetchSightingWithPage" method="GET">
            <input style="width: 230px" type="datetime-local" name="startTime" id="startTime" >
            <input style="width: 230px" type="datetime-local" name="endTime" id="endTime">
            <input style="width: 230px" type="text" name="witnesses" id="witnesses" placeholder="username" value="<%= witnesses %>">
            <button style="width: 135px" type="submit">Search</button>
        </form>
    </div>
    <form action="/logout" method="POST" id="login_form">
        <div class="exit-container">
            <button class="exit-btn" type="submit">Exit</button>
        </div>
    </form>
</div>

<table>
    <caption>Sightings List</caption>
        <tr>
            <th>ID</th>
            <th>Time</th>
            <th>Identification</th>
            <th>Location</th>
            <th>User</th>
        </tr>
    <tbody>

       <% var id = (page - 1 ) * 10; %>
       <% for (var i = 0; i < birds.length; i++) { %>
           <tr>
               <td><%= id + i + 1 %></td>
               <td><%=birds[i].time%></td>
               <td><%=birds[i].identification%></td>
               <td><%=birds[i].location%></td>
               <td><%=birds[i].witnesses%></td>
        </tr>
    <% } %>
    </tbody>


</table>
<div class="center">
    <ul class="pagination">
        <% for(var i=1; i<=total; i++) { %>
            <li><a href="/sighting/fetchSightingWithPage?pageNum=<%= i %>"><%= i %></a></li>
        <% } %>
    </ul>
</div>

</body>
</html>
